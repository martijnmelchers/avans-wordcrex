package model.matchfixer;

import model.database.DocumentSession;

public class Matchfixer {
    public boolean invitePlayer(String Player){
        DocumentSession.getDatabase().update();

        return false;
    }
}
